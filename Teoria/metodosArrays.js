// .push()   agraga objetos al array  , se usan parametros

// .pop()   elimina el ultimo elemento de un array y devuelve el objeto eliminado

// .shift() elimina el primer elemento de un array y devuelve el eliminado

// .unshift()  agrega elementos al array se usan parametros

// .join()  une los elementos de un array como si fuera una string  y se puede especificar como se separa

// .indexOf()  busca en el array el elemento especificado. Si no lo encuentra da -1 

// .lastIndexOf()  mismo que el anterior pero de atras hacia adelante

// .incluides() es como el anterior pero devuelve un boooleano si encuentra o no el elemento buscado



//Ciclos

/* for(){}

variable inicializadora
condicion a evaluar (cuantas interaciones )
modificador de la variable inicializadora */

// ejemplo

for(let i = 0; i<10; i++){
    console.log('El valor de i es igual a: ' + i);
}


